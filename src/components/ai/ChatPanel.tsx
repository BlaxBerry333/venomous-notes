'use client'

// Phase 4: useChat (Vercel AI SDK), streaming bubbles, localStorage history
export function ChatPanel() {
  return (
    <div
      style={{
        flex: 1,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '13px',
        color: 'var(--muted2)',
      }}
    >
      Chat — Phase 4
    </div>
  )
}
